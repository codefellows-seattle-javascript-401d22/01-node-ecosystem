* In greet.js the single function is export a function that has an airity of 1 and expects a string as its input. If the input is valid, it will return a message saying 'hello [value of input]'. If the input is not valid, it will return null.

* In arithmatic.js, one function that is exported is add which has an airity of 2. It expects two numbers as the inputs. If it is valid, it will return the sum of the two numbers. If one or both inputs are not numbers, it will return null.

* The other function in arithmatic.js is sub which has an airity of 2. It expects two numbers as inputs. If the inputs are valid, it will return the difference between the two numbers. If one or both inputs are not numbers, it will return null.